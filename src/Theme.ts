import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
          main: '#1a237e', // Change this to your primary color
        },
        secondary: {
          main: '#ff5722', // Change this to your secondary color
        },
      },

    components:{
        
    }
})

