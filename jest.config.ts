module.exports = {
  testPathIgnorePatterns: [
    "<rootDir>/node_modules/",
    "<rootDir>/build/",
    "<rootDir>/dist/",
  ],
  verbose: true, // Optional: Enables verbose output for test runs
  silent: true,
};
